function BattleBackgroundData(_sprite, _progress, _length, _frequency, _horizontal, _split, _ripple, _sub) constructor {
	battleBackground = _sprite
	
	progress = _progress
	length = _length
	frequency = _frequency

	horizontal = _horizontal
	split = _split
	ripple = _ripple

	transparent = _sub
}